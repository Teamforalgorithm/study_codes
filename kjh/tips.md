# Good tips to code

## Python

- use sys.stdline.readline

## Cpp

- use `\n` instead of `endl`
- use this code on the top:

```cpp
ios_base :: sync_with_stdio(false);
cin.tie(NULL);
cout.tie(NULL);
```
